import { CardProps } from ".";
  import type { StateType } from "./state";
  import type { HelperType } from "./helper";

export type LogicType = {
};

export const CardLogic = (state: StateType, props:CardProps, helper?: HelperType): LogicType => {

   return {}
};