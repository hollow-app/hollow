export type Realm = {
        id: string;
        name: string;
        createdDate: string;
        lastEntered: string;
        colors: {
                primary: string;
                secondary: string;
        };
};
