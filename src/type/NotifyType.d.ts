export type NotifyType = {
        id: string;
        title: string;
        message: string;
        attachment?: string;
        submitted_at: string;
        expires_at: string;
        type: string;
        visible?: boolean;
};
