import { ContainerProps } from ".";
import type { StateType } from "./state";
import type { HelperType } from "./helper";

export type LogicType = {};

export const ContainerLogic = (
	state: StateType,
	props: ContainerProps,
	helper?: HelperType,
): LogicType => {
	return {};
};
